# 0.11.0输入更新

左Alt现按一次开启，再按一次关闭；不再要求按住。关闭立即取消剩余作业，进入世界默认关闭。G仍切换范围／矿脉。农业两项实现和测试边界见[P3农业设计](P3_AGRICULTURE_DESIGN_zh-CN.md)。下文0.10.0输入规则为历史记录。

# 已批准P3批次与0.10.0实现边界

2026-09-14：用户确认P3五项天赋分两批方案，随后批准把鞭子、链锤攻击范围补全放入第一批，并明确开始开发。基于已合并PR #12：`308621510075c85188bdc546657c4d7a0433f354`。

## 本批

| 项目 | 数值／费用 | 边界 |
|---|---|---|
| 范围挖矿 | 每级1点、半径+1；Lv1=3×3，Lv10=21×21 | 无限升级、强度可调；按选中模式执行 |
| 矿脉连锁 | 每级1点、同种矿石25格；Lv10=250 | 包含首格，8方向连通，不跨空隙 |
| 一键伐木 | 一次2点 | 砍断树干后完成同一棵原版树，包括下部树桩，不连锁森林 |
| 公共保护 | 不另收费 | 服务器权威、工具／距离检查、保护对象及支撑、总开关与预算 |
| 攻击范围补全 | 沿用MeleeRange，+10%/级 | 原版鞭子、Flail AI链锤；不放大附加弹幕或未知MOD投射物 |

原有关闭不退款、退款返还实际投入规则保持。0.10.0实现后90项天赋，存档v3；新增服务器世界操作意图消息，协议12。

## 输入与授权

按住可配置批量键（默认LeftAlt）使用工具，G切换范围／矿脉；斧头在原版树干上自动用伐木。菜单、聊天、鼠标UI时不启动。普通使用保持原生流程。

批量输入由服务器接收角色会话、天赋修订、递增请求号、鼠标方块、模式和当前工具槽位／类型。服务器自己读取镐力／斧力／天赋，校验存活、距离、节奏及许可，按原生累计伤害挖首格。客户端不传破坏成功或奖励数量，也不自行破坏首格。单人复用同一入口。

首格未挖掉时不创建额外作业。队列每人最多一个，全服默认32个工作单位/帧轮转处理；每次检查候选或敲击一次计工作。每次默认最多1000格包含首格，0不限制总量，仍分帧。切工具、角色离开、修改天赋、关闭许可或离开首格可达距离时停止后续工作。

## 世界与兼容性

范围以鼠标格为中心，同心方形由近到远处理。只采集普通原版实心地形；保守排除frame-important对象、家具、树、机关、电线／执行器、地牢／神庙砖、下落方块及其支撑。保护所有方向紧邻的frame-important对象支撑，保留原生CanKillTile和镐力检查。

矿脉按Ore标记和同TileType连通。MOD矿石默认关闭，可由服务器明确开启；仍使用其镐力／破坏规则。无需执行任何未知饰品逻辑。未知MOD世界操作副作用未保证兼容。

伐木按原生树帧和GetTreeBottom识别同树，处理树根、树干和原生树冠块；普通树、棕榈、宝石树等属于原版树干集合。原版底部砍树本已倒树，本功能补齐从中段开砍后的下部树桩处理。不含巨型蘑菇、仙人掌、竹子、MOD树和木材建筑。整树包含受保护方块或数量超预算，启动前拒绝整棵操作。

每次WorldGen.KillTile调用限制到本次已检查方块，阻止原生framing递归破坏旁格绕过预算。落地物品继续经过EconomySystem的实际操作者和真实TileBreak上下文；产量只计算一次，不创建第二次客户端奖励。

保护矩形使用方块X/Y/Width/Height，由服务器设置；仅影响本MOD批量采集，不替代第三方领地保护系统。

## 武器

鞭子通过原生GetWhipSettings调整RangeMultiplier，绘制和碰撞共享控制点，不修改伤害、时间、标记或附加召唤效果。链锤通过FlailStats调整旋转、发射距离和对应回收速度，通过FlailSpinCollisionRange匹配旋转命中；使用原生撞墙、落地和回收AI。

原版链锤超过900像素离身距离会被清除。按原生发射速度、时间及攻速计算允许倍率，对极端输出饱和，保留等级／付款不变。普通挥砍、短剑、长矛旧适配继续保留。

## 下一批，尚未实现

范围收割：每级1点，半径+1，Lv10=21×21；自动补种：一次2点。第一轮只覆盖原版草药，优先能产种子的成熟阶段；使用背包或本次合法收获的对应种子，无种子仍可收割并跳过补种，失败不扣材料。补种也能配合单株收割。攻击破坏地形、拆墙／建筑扩展仍是候选。

自动检查与实机验收分开。真实多人、特殊Boss、旧未验收天赋及多人私人掉落等不自动改变状态。

## 自动验证与交付记录

功能代码提交 `d475f70e8a6defe9bd845884dbdb3290abd4bc0e` 已通过[分支CI 34825975807](https://github.com/zhu530319195-max/TerrariaProgression/actions/runs/34825975807)和[PR CI 34825980551](https://github.com/zhu530319195-max/TerrariaProgression/actions/runs/34825980551)：3150项核心、651项原生检查通过，生产与测试MOD编译均为零警告、零错误。

2026-09-14：用户确认0.10.0本批验收通过，并明确授权合并[PR #13](https://github.com/zhu530319195-max/TerrariaProgression/pull/13)。验收包提交 `95e55b29156bad921e77d4d3bef1f1c701d9f359`；本次只更新验收文档，功能代码与验收包一致。合并提交以PR记录为准。

验收提交已通过[分支CI 34826265788](https://github.com/zhu530319195-max/TerrariaProgression/actions/runs/34826265788)和[PR CI 34826269107](https://github.com/zhu530319195-max/TerrariaProgression/actions/runs/34826269107)：3150项核心、651项原生检查通过，生产与测试MOD编译均为零警告、零错误。

验证覆盖：首格原生镐力累计、3×3及同种8方向连矿、草皮转泥土后的继续挖掘、单次与分帧预算、箱子／支撑／电线／保护矩形、关闭／退款／换工具取消、服务器会话与槽位／距离／重放检查、同树与邻树区分、实际生成树冠／树枝和棕榈沙地、正常木材和棕榈木产量只增强一次、三种原版鞭子的可见控制点和命中、四种链锤的旋转／甩出／回收与锤头尺寸，以及已有天赋回归。

实际棕榈TileID.PalmTree不在原版通用IsATreeTrunk集合内，本批作明确适配；木材产量同步纳入棕榈木。世界保护允许树下有正常沙地，但仍禁止挖掉上方沙块的支撑。批量伐木未适配特殊斧头的自动种树／语音等附带行为，农业补种仍属下一批。

真实多人实机、客户端画面和实际操作手感仍以用户反馈为准。较高强度叠加高攻速时，链锤可能先触及原版离身距离保护，实际倍率会饱和；等级和实际投入不变。
